export const slugUrl = ":slug([a-z-0-9]+)/:id([0-9]+)/";
export const baseUrl = "/";
export const searchUrl = `${baseUrl}search/`;
export const DASHBOARD = `${baseUrl}dashboard`;
export const PROFILE_PAGE = `${baseUrl}dashboard/profile`;
export const CONTACT = `${baseUrl}contact`;
export const HOME = `${baseUrl}home`;
export const TOS = `${baseUrl}terms-and-conditions`;
export const SDG = `${baseUrl}sustainable-development-goals`;
export const VACANCIES = `${baseUrl}vacancies`;
export const PROFILE = `${baseUrl}p`;
export const RESUME = `${baseUrl}r`;
export const CATEGORIES = `${baseUrl}categories`;
export const PRICING = `${baseUrl}pricing`;

export const ACCOUNT = `${baseUrl}account`;
export const ADMIN = `${baseUrl}/admin`;
export const PASSWORD_FORGET = `${baseUrl}pw-forget`;
export const PASSWORD_RESET = `${baseUrl}pw-reset`;
export const EMAIL_VERIFY = `${baseUrl}email-verify`;
export const WELCOME = `${baseUrl}welcome`;

export const LANDING = `${baseUrl}`;
export const APP = `${baseUrl}apps`;
export const AUTH = `${baseUrl}auth`;

export const SINGLE_VIEW = `${baseUrl}dashboard/view`;
export const JOBS = `${baseUrl}dashboard/jobs`;
export const MYJOBS = `${baseUrl}dashboard/my-jobs`;
export const GIGS = `${baseUrl}dashboard/gigs`;
export const MYGIGS = `${baseUrl}dashboard/my-gigs`;
export const INTERNSHIPS = `${baseUrl}dashboard/internships`;
export const MYINTERNSHIPS = `${baseUrl}dashboard/my-internships`;
export const APPLICATIONS = `${baseUrl}dashboard/applications`;
export const BILLING = `${baseUrl}dashboard/billing`;
export const HELP_PAGE = `${baseUrl}help`;
export const ABOUT = `${baseUrl}about`;
export const TERMS_CONDITIONS = `${baseUrl}terms-and-conditions`;
